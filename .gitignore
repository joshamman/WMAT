# Brand source files — large Illustrator originals & raw concept exports.
# Kept locally only; export web-ready assets into assets/ instead.
/brand-source/

# Packaged theme zip (build artifact)
*.zip

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Node (if we add a build step later)
node_modules/
npm-debug.log*

# Local WP / env overrides
.env
*.local

# Local static preview (regenerated, not shipped)
home-preview.html

# Unrelated personal files kept in this working copy
/Amy Hamman - AATA 2026 CONFERENCE PROPOSAL SUBMISSION - Google Docs.pdf
/Hospice Document.md
